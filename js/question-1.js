// function getRemainder(a,b) {
//   return a % b;
// }

// for multi line functions

// eslint-disable-next-line arrow-body-style
const doModulus = (a, b) => {
  return a % b;
};

// for single line fiunctions

const getRemainder = (a, b) => a % b;

getRemainder(99, 30);
doModulus(50, 40);
